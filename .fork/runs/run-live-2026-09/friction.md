# Friction log: living in the build

**Horizon: `.fork/GOAL.md`, set 2026-09-04.** One dated line per thing that
stopped a turn or annoyed enough to notice. An empty day is a line saying so.
The count ranks; the newest line is not the most important.

Launched through `warpdev.ps1`'s product profile unless a line says otherwise.

| date | # | what | stopped work? | where it went |
|---|---|---|---|---|
| 2026-09-05 | 1 | Opened the diff panel in a WSL pane on this repo: *"Cannot detect diffs for this folder. Diffs don't currently work in WSL."* | yes | `.fork/docs/wsl.md`; board item 7. Step 1 (connect automatically) built the same day, `ea61116e1` + `1a42ecdb8`; step 2 the same evening, `099b26ea5`: routed, the panel works unchanged; unrouted, the text names the 9p read |

## The agent's log

What the agent working `.fork/next.html` lacked, one line per session, in
the same shape. Started 2026-09-07 as I19's first step, done as a habit.

| date | # | what | stopped work? | where it went |
|---|---|---|---|---|
| 2026-09-07 | a1 | Named `opencode acp` for a launch from the Windows build and every turn failed at `initialize` with *Incoming transport closed*; the panel said nothing about why. The login shell had no nvm on its PATH | yes | the absolute path; `.fork/docs/manual.md`, "Naming an agent that lives under nvm". Open: the panel could show the agent's stderr on a transport that closes at `initialize` |
| 2026-09-07 | a2 | Declared a loopback Custom Inference endpoint the way the manual said and the four small features answered *"no Custom Inference endpoints are configured"*; the form refused the URL too. Three stale points from upstream's 2026-08-26 merge, one under the other | yes, an hour | `7529749a8`, `26c376090`; `T03`, `CLAUDE.md`'s fourteenth. Open: a test that goes through upstream's door, so the next merge cannot do this silently |
| 2026-09-07 | a3 | Two release-build launches on a scratch profile ran on the real profile: `WARP_DATA_PROFILE` is debug-only and a relocated `LOCALAPPDATA` moves the discovery record and nothing else | yes, two builds | the debug binary, as the cancel run had already found; `run.sh` says why. Open: the manual's scratch-profile recipe is Linux-only |
| 2026-09-07 | a4 | The commit dialog in a routed WSL pane stayed blank with the local model configured: the daemon generates the message in a process that has no fork config | no | `next.html`, the runtime item; `wsl.md` diff-panel row. Fix is a protocol addition (daemon returns the diff, GUI generates) |
| 2026-09-07 | a5 | `keys.ps1 -Key Plus -Ctrl -Shift` typed `=` into the input; posted modifier key-downs are not modifier state | no | `warpctrl surface code-review open`, or the footer chip; the manual's script table |
| 2026-09-07 | a6 | Told the maintainer the WSL image was on C: and to `wsl --manage --move` it to X:. It was already on X: behind Store-relocation junctions; the move lifted the vhdx out of app-container EFS and WSL could not attach it. My note had recorded "988 GB on a 931 GB drive" without stopping at a size that cannot fit, and a search of X: that never looked under `X:\WpSystem` | yes, the maintainer's evening and nearly the distro | repaired by another session, `.fork/runs/wsl-move-2026-09-07/`; `CLAUDE.md`, *Method*. Rule: never `--move` this distro; a size that cannot fit its drive is a reading error |
| 2026-09-09 | a7 | Working from the mobile surface, the running Warp *is* the surface, so every board item that needs a relaunch is out of reach from it. Item 3 of `next.html`, a local model answering the panel, is the top open item and needs exactly that | no, but it decided the day's scope | this line. **Premise falsified the same day** (`.fork/runs/remote-launch-2026-09-09/`): Warp relaunches from an SSH session in 3 s with the desk locked, `warpctrl` writes land, and `shot.ps1` returns the full window, so the panel-side half is reachable from a phone after all. `.fork/HANDOFF-LOCALMODEL.md` is the run this unblocks. **Run completed the same day** (`.fork/runs/localmodel-panel-2026-09-09/`): item 3 measured end to end from this session, no fork code touched |
| 2026-09-09 | a8 | Could not verify whether the drift-check cron line is installed: `crontab -l` was refused by the harness, and no file in the repo records the answer. Three files assert it is not installed and none of them can know | no | `board.html`'s pointer was corrected; the "not yet installed" half is left standing as unverifiable from here |

## Notes

**2026-09-05, #1.** Found within minutes of the first product-profile session.
The pane was probably not routed (nothing connects the server automatically,
and `session inspect` was not run), so this is the 9p column of the state table
in `.fork/docs/wsl.md`: detection walks the repo from Windows and never
finishes, the panel has no repository, and the fallback text blames WSL. The
maintainer restated the requirement the same day: WSL is a remote server, and
nothing user-facing goes through 9p. Diffs, the editor's language servers and
the clipboard are the surfaces named.
| 2026-09-09 | a9 | Closed the phone's Firefox tab and could not get back into the console; it read as Warp being fragile. Measured: Warp never went down (overnight pid still up, console answered 200, pairing held), and the block was the phone's Tailscale DNS making the web layer flaky. The real fragility is that the console is a browser onto a GUI, neither relaunchable from the phone | no | manual "Sessions die when the link drops" rewritten: durable remote work is a CLI agent in tmux over mosh, console is the desk view; reach.html gets the "Use Tailscale DNS off" fix for slow web |
| 2026-09-09 | a10 | The local model refused every panel turn with `request (62960 tokens) exceeds the available context size (12288 tokens)`, which reads as the model being too small and is not. `serve.ps1` defaults to `-Ctx 12288`; this repo's `CLAUDE.md` and its included skill are ~42,000 tokens of every request, on top of Claude Code's own 20,860-token floor | yes, the first attempt | `-Ctx 98304`, which fits in 12 GB because Gemma's sliding-window attention costs ~16 MiB per 1k tokens. Passed per launch, so a restart silently breaks the panel again. Open: whether `serve.ps1`'s default should change, the maintainer's call |
| 2026-09-09 | a11 | **Answered a10**: the maintainer said change it, so `C:\dev\llama\serve.ps1` now defaults to `-Ctx 98304` with the reason in its own `.DESCRIPTION`. Untracked machine config, so the run README is the only record | no | `.fork/runs/pricefetch-2026-09-09/` |
| 2026-09-09 | a12 | Wrote a background waiter as `until ! pgrep -f 'cargo check --workspace'; do sleep 10; done`. It never fired: the `bash -c` running the loop has that string in its own argv and matches itself. `CLAUDE.md` records this exact trap from 2026-08-30 and I wrote it anyway, having read the file this session | no, ~4 minutes | killed and run directly. The rule is in the file; what is missing is a waiter that cannot make the mistake. A helper that waits on a *pid* or a marker file, not a pattern, would retire it |
| 2026-09-09 | a16 | `cargo clean --release` for the item 7 measurement broke the maintainer's WSL panes: `~/.warp-dev/remote-server/warp-oss` is a **symlink into `target/release`**, so the daemon had nothing to exec and Warp showed *"Failed to start SSH extension. Response channel closed before receiving a reply"* — a transport error naming neither WSL nor the daemon. Found by the maintainer looking at their own screen, an hour after I wrote "nothing depends on it" in the run README | yes, for them; file browsing and code review degraded to 9p until a rebuild | repair build (one crate, deps warm, ~6 min). Recorded in `CLAUDE.md`, `.fork/docs/wsl.md` and the run README. `ls -la ~/.warp-dev/remote-server/` shows the dangling link and settles it in one command |
| 2026-09-09 | a15 | Started the clean-build test of `-j 8` after checking `free -m` **inside the guest** (22 GB available, reads as a green light). The host was at **60 GB of 64**; the maintainer said so and that is how it was caught. `CLAUDE.md` names this exact error one paragraph from the one that authorised the build: the hazard is the host's memory, not the VM's | yes, the build was abandoned part-way and `target/release` is empty | build stopped, 15.4 GB freed by killing my own session's rust-analyzer. `CLAUDE.md` now carries the `powershell.exe Get-CimInstance` one-liner that reads the host from inside WSL, so the rule is a command rather than an exhortation |
| 2026-09-09 | a15 | Started the clean-build test of `-j 8` after checking `free -m` **inside the guest** (22 GB available, reads as a green light). The host was at **60 GB of 64**; the maintainer said so and that is how it was caught. `CLAUDE.md` names this exact error two paragraphs from the one that authorised the build: the hazard is the host's memory, not the VM's | yes, the build was abandoned part-way and `target/release` is empty | build stopped, 15.4 GB freed by killing my own session's rust-analyzer. `CLAUDE.md` now carries the `powershell.exe Get-CimInstance` one-liner that reads the host from inside WSL, so the rule is a command rather than an exhortation |
| 2026-09-09 | a14 | **`.fork/tools/memsample.sh` has been counting `rust-analyzer` as a compiler.** `ps -C rustc` is not an exact match on this procps and also selects `rust-analyzer`, which over this workspace holds **16.5 GB** — so every sample taken while an editor was open added 16 GB to `sum_rss_mb`, put 16,098 in `max_rss_mb` and inflated `n_rustc` by one. Caught mid clean-build; the corrected selection showed the largest real rustc at 847 MB at that moment | no, but it would have produced a wrong answer to the `-j 8` question this run exists to settle | fixed to `ps -p $(pgrep -x rustc)`. **The tell was in the output for twenty minutes and I read past it**: rust-analyzer has no `--crate-name`, so the crate column said `?`. A row whose `max_crate` is `?` is not describing a compiler. I then wrote up the contamination as casting doubt on `CLAUDE.md`'s 2026-09-04 figures and **that was wrong too**: those rows name the `warp` crate in their peak sample and a rust-analyzer cannot, so the `?` is the discriminator both ways. The corrected sampler reached the app crate before the build stopped and confirmed it at 14,975 MB. Retracted within the hour |
| 2026-09-09 | a13 | The socket poller used for the phone-home measurement never caught its own positive control -- a `node` process connecting out produced no row in three attempts, including one holding the socket open five seconds, while `ss -tnp` attributed every `claude` socket correctly. Under WSL2 mirrored networking the guest's view of its own sockets is partial | no; both findings were presences, which survive a blind instrument | recorded in the run README as a limitation. **Any absence claim must come from the 2026-09-09 panel run, whose control did fire, not from this one.** Open: why `ss` attributes some processes and not others under mirrored networking |
