warpdev: ~/.warpdev is no longer read (profiles are per launch); delete it when convenient.
warpdev: profile INSTRUMENTED (the rig) + CONSOLE
  WARP_FORK_ACP_COMMAND    = wsl.exe -d Ubuntu -- npx -y @agentclientprotocol/claude-agent-acp@0.73.0
                             the agent panel answers from this agent, started inside WSL so a pane cwd resolves
  WARP_FORK_EVENT_LOG      = on
                             one JSONL per conversation, the record the console draws a run from
  WARP_FORK_ACP_MODE       = default
                             makes the agent ask; without it its own classifier answers and Warp is never in the loop
  WARP_FORK_TRANSCRIPT     = on
                             the conversation on disk under the pane directory, for grepping back what compaction dropped
  WARP_FORK_CONTROL_BIND   = 192.168.254.3:41234
                             the wide listener, so a phone on the LAN can pair; the only variable that reaches off the machine
  note: a Warp started any other way gets none of this.
warpdev: binary C:\dev\warp\target\release\warp-oss.exe
warpdev: tree at 0fedbfda5 fork: four things the emulator found on the page, fixed the same night (T19)
warpdev: launching INSTRUMENTED (the rig) + CONSOLE
warpdev: up - inst_5cc8318d96bd4a21bc3f7274ba376564 (pid 37652, channel warp-oss, app dev.warp.WarpOss, protocol 1)
